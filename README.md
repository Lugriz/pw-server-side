# Pdfmake-wrapper on server-side

This is an example of using pdfmake-wrappe on server-side.

To run the app just install the dependencies:

```bash
npm install
```

and run the app:

```bash
npm start
```
